package ru.atom.geometry;

public class createBar extends Bar {
    public createBar(int firstCornerX, int firstCornerY, int secondCornerX, int secondCornerY) {
        super(firstCornerX, firstCornerY, secondCornerX, secondCornerY);
    }
}
